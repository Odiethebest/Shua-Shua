# web/ — React frontend (Milestone M4, planned)

Not part of the current milestone. The frontend is presentation only: a
Xiaohongshu-style masonry feed, a persona switcher, a per-card "why recommended"
line, and a collapsible DAG panel that animates the per-operator trace emitted by
the engine.

See the root `README.md` (Architecture, Roadmap) for the design. Nothing here
yet — this directory is a placeholder so the project layout is visible from M0.
